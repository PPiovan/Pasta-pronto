En los modelos podemos encontrar como los campos de la tabla y el tipo de dato que tiene
Se le debe asignar  
**NOMBRE POR EJEMPLO** id_configuracion: {
    type: DataTypes.INTEGER,
    primaryKey: true,
    autoIncrement: true,
  },

  nombre de la tabla por ejemplo configuracion_reservas
  y un timestamp  a data type used to store a specific point in time, typically including both the date and the time of day

  La saque de google para que tengan una idea

  Agregue ademas de eso unos modulos llamados nodemon que supuestamente por lo que lei 
  reinicia automáticamente la aplicación al detectar cambios en los archivos fuente

  Sumado a eso CORS y Express

  CORS mecanismo de seguridad de los navegadores web que restringe las solicitudes HTTP entre diferentes dominios

  y bueno express para el back


  Por otra parte se agregaron las rutas y los controladores en donde

  RUTAAS son las peurtas de entrada por ejemplo
  router.post("/reservas", crearReserva);
  Cuando alguien haga POST a /reservas ejecutá crearReserva


  CONTROLADORES Son la lógica del negocio

  export const crearReserva = async (req, res) => {
  const nueva = await Reserva.create(req.body); // Con el await esperamos que se cree una reservas y espera una request por eso req.body
  el .body es pq el cliente lo manda directamente del body

  res.json(nueva);
    };

res.json(nueva) lo que hace es pasar como json esa request q se mando 